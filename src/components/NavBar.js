import React from "react";

function NavBar() {
  // update the JSX being returned!
  return (
  <nav>
    <a herf="#home">Home</a>
    <a herf="#about">About</a>
  </nav>
  );
}

export default NavBar;
